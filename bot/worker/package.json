{
  "name": "planner-qa-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker — blumnAI-qa-bot 백엔드. 정책 md fetch + Anthropic 스트리밍 + qa/decisions·feedback 저장.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types",
    "test": "vitest run",
    "check": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}

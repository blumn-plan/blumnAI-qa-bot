#!/usr/bin/env node
/**
 * npx create-blumnAI-qa-bot <target-dir>
 *
 * 사용자 레포를 init 하는 스캐폴더 CLI.
 * TODO (6단계에서 구현):
 *   1. CLI prompt — team_name / github_repo / project id 등 묻기
 *   2. target-dir 에 디렉토리 골격 생성
 *   3. templates/config.yml.template 을 입력값으로 채워서 복사
 *   4. .blumnAI-qa-bot/ 에 apps/ + worker/ 사본 배치
 *   5. .github/workflows/deploy.yml 생성
 *   6. (선택) Worker / Pages 배포까지 자동 수행
 *
 * 현재는 placeholder.
 */

console.log('blumnAI-qa-bot CLI — 6단계에서 구현 예정');
console.log('수동 셋업 방법은 docs/01-INSTALL.md 참고');
process.exit(0);
